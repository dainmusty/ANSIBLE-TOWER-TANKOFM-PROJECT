---
plugin: aws_ec2
regions:
  - us-east-1
filters:
  tag:env: dev
keyed_groups:
  - key: tags.role
    prefix: role
  - key: tags.install
    prefix: install
hostnames:
  - public-ip-address
compose:
  ansible_host: public_ip_address

# 👇 Debug option
strict: False
