---
plugin: aws_ec2
regions:
  - us-east-1     # adjust if needed
filters:
  tag:Environment: dev
keyed_groups:
  - key: tags.role
    prefix: role
hostnames:
  - private-ip-address
compose:
  ansible_host: private_ip_address
